# frontend-design skill

This folder contains a local scaffold of the `frontend-design` Anthropics skill.

## Install / usage

- Keep this folder in your repo at `skills/frontend-design`.
- If you have an agent or tool that loads skills, point it to this folder or merge the files into your skill directory.
- Customize `skill.yaml` fields (`maintainer`, `version`, `inputs`) to match your environment.

If you want, I can:

- fetch a remote version if you provide a URL
- enhance the skill with example React components or Tailwind snippets
